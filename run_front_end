#! /bin/bash

mkdir -p course

#./run_container front_end "-e NUM_PROCESSES=4 -e PORT=9001 -v $(pwd)/course:/course -v $(pwd)/data:/data -v /tmp:/tmp" ""
./run_container front_end "--network host -e NUM_PROCESSES=4 -e PORT=9001 -v $(pwd)/course:/course -v $(pwd)/data:/data -v /tmp:/tmp" ""
#--tmpfs /tmp
